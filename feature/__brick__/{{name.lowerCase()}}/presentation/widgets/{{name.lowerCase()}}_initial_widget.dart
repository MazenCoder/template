import 'package:flutter/material.dart';



class {{name.pascalCase()}}InitialWidget extends StatelessWidget {
  const {{name.pascalCase()}}InitialWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
